/**
 * ============================================================================
 * 📘 在森林那邊 - 系統技術維護手冊 (Developer Guide)
 * ============================================================================
 *
 * # 專案概觀
 * - **版本：** V2.3 (2026/01/23)
 * - **狀態：** Production
 * - **功能：** 輕量級電商庫存管理系統
 *
 * ---
 *
 * ## 1. 檔案結構總覽 (File Structure)
 *
 * ### 📱 Mobile Web App
 * | 檔名 | 類型 | 說明 |
 * | :--- | :--- | :--- |
 * | **`Code.gs`** | API | Web App 入口。實作 `LockService` 並支援 `triggerManualImport` 手機觸發匯入。 |
 * | **`index.html`** | UI | 包含 User Selector、Toast 通知、**匯入按鈕**。CSS 針對 Mobile 優化。 |
 *
 * ### ⚙️ Backend Logic
 * | 檔名 | 類型 | 說明 |
 * | :--- | :--- | :--- |
 * | **`ShopeeTextParser.gs`** | Parser | 解析蝦皮文字，支援提取 **物流單號**。 |
 * | **`WooCommerceParser.gs`** | Parser | 解析官網訂單 (HTML Table 格式)。 |
 * | **`InventoryManager.gs`** | Core | `refreshDashboard()`: 重算庫存總表。 |
 * | **`Archiver.gs`** | Bot | `autoArchiveSalesData()`: 歷史資料封存機器人。設定為 Time-driven Trigger (每月執行)。 |
 * | **`main.gs`** | Batch | `generateDailyPickingList()`: 每日訂單處理核心。**支援合併訂單顯示**。 |
 *
 * ---
 *
 * ## 2. 關鍵機制說明
 *
 * ### A. 並發控制 (Concurrency Control)
 * - 所有的寫入函式 (`submitProduction`, `adjustInventory`) 皆包覆在 `LockService` 中。
 * - 邏輯：`tryLock(10000)` -> 執行寫入 -> `finally { lock.releaseLock() }`。
 * - 確保多人同時操作時，庫存計算不會產生 Race Condition。
 *
 * ### B. 自動封存 (Auto Archiving)
 * - 閾值：保留 `[03_銷售數據池]` 最新 **2000** 筆。
 * - 動作：超過部分搬移至 `[Backup_Sales]` 並從主表刪除。
 * - 目的：防止 Google Sheet 因資料量過大導致 `refreshDashboard` 超時 (Timeout)。
 *
 * ### C. 組合商品過濾
 * - 入庫選單 (`getSkuList`) 與庫存列表 (`getInventoryStatus`) 皆會過濾：
 * 1. `Status` 為 `Soft_Delete` 或 `EOL`。
 * 2. `Category` 為 `組合`。
 *
 * ---
 *
 * ## 3. Schema 對照
 *
 * ### `[04_SKU對照表]`
 * | Index | 欄位 | 用途 |
 * | :--- | :--- | :--- |
 * | 5 (F) | Status | Active / Soft_Delete |
 * | 6 (G) | Category | 商品 / 原料 / 包材 / 組合 (前端分組依據) |
 * | 7 (H) | SafetyStock | 安全庫存水線 (前端紅燈依據) |
 *
 */

function README() {
  // Documentation only.
}